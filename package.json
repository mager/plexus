{
  "name": "plexus",
  "version": "0.1.0",
  "description": "Lightweight harness wiring chat channels (Telegram, CLI, more) to Claude with smart model routing.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "cli": "PLEXUS_CHANNELS=cli bun src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "latest",
    "@anthropic-ai/sdk": "latest",
    "grammy": "^1.30.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
